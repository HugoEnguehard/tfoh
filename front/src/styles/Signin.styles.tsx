// Material imports
import { Grid, styled } from "@mui/material";

export const CustomGrid = styled(Grid)({
    minWidth: '500px',
    width: '40%',
    backgroundColor: '#FFFFFF',
    padding: '10px',
    borderRadius: '10px',
});